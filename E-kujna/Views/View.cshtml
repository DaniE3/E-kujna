@model IEnumerable<E_kujna.Models.Obrok>

@{
    ViewBag.Title = "View";
}

<h2>View</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ObrokIme)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ObrokIme)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ObrokID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ObrokID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ObrokID })
        </td>
    </tr>
}

</table>
